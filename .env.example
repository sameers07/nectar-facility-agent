GEMINI_API_KEY=

# Optional overrides -- sensible defaults are used if unset.
# GEMINI_BASE_URL=https://generativelanguage.googleapis.com/v1beta/openai/
# GEMINI_MODEL=gemini-2.5-flash        # investigation model for low-complexity requests
# GEMINI_STRONG_MODEL=gemini-2.5-pro   # investigation model for high-complexity requests
# GEMINI_ROUTER_MODEL=gemini-2.5-flash # request router (always cheap/fast, independent of complexity)
# WHISPER_MODEL=base                   # local STT model size (base/small/medium/...)
# AUDIO_INPUT_DEVICE=                  # substring match to override the default mic (see README)
